<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Ephrem Bzuayehu Asmamawu - CV</title>
<style>
  :root {
    --primary-color: #0d3b66;
    --secondary-color: #1d3557;
    --accent-color: #a8dadc;
    --text-color: #222;
    --background-color: #f9fbfc;
    --card-background: #fff;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: var(--background-color);
    color: var(--text-color);
    line-height: 1.6;
    padding: 20px;
  }

  .container {
    max-width: 900px;
    background: var(--card-background);
    margin: 0 auto;
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
  }

  header {
    margin-bottom: 2rem;
    border-bottom: 3px solid var(--primary-color);
    padding-bottom: 1rem;
  }

  h1 {
    font-size: 2.5rem;
    color: var(--primary-color);
    margin-bottom: 0.5rem;
  }

  h2 {
    color: var(--secondary-color);
    margin: 2rem 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid var(--accent-color);
  }

  .contact-info {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-top: 1rem;
    font-size: 0.9rem;
  }

  .contact-info > * {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  a {
    color: var(--secondary-color);
    text-decoration: none;
    font-weight: 600;
    transition: opacity 0.3s ease;
  }

  a:hover {
    opacity: 0.8;
    text-decoration: underline;
  }

  .section-item {
    margin: 1.5rem 0;
  }

  .job-title {
    font-weight: 700;
    font-size: 1.1rem;
  }

  .date-location {
    display: block;
    font-style: italic;
    color: #666;
    margin: 0.3rem 0;
    font-size: 0.9rem;
  }

  ul {
    padding-left: 1.5rem;
    margin: 0.8rem 0;
  }

  .skill-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    margin: 1rem 0;
  }

  .skill {
    background: var(--accent-color);
    color: var(--secondary-color);
    padding: 0.4rem 1rem;
    border-radius: 15px;
    font-size: 0.9rem;
    font-weight: 500;
  }

  .download-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--primary-color);
    color: white;
    padding: 0.8rem 1.5rem;
    border-radius: 8px;
    margin: 1rem 0;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .download-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(13, 59, 102, 0.3);
  }

  .lang-switcher {
    display: flex;
    gap: 0.5rem;
    justify-content: flex-end;
    margin-bottom: 1.5rem;
  }

  .lang-btn {
    padding: 0.5rem 1rem;
    border: none;
    border-radius: 6px;
    background: var(--accent-color);
    cursor: pointer;
    transition: background 0.3s ease;
  }

  .lang-btn.active {
    background: var(--primary-color);
    color: white;
  }

  @media (max-width: 768px) {
    .container {
      padding: 1.5rem;
    }

    h1 {
      font-size: 2rem;
    }

    .contact-info {
      flex-direction: column;
    }
  }

  @media print {
    .lang-switcher {
      display: none;
    }
    
    .container {
      box-shadow: none;
      padding: 0;
    }
  }
</style>
</head>
<body>
  <div class="container">
    <div class="lang-switcher">
      <button class="lang-btn active" data-lang="en">EN</button>
      <button class="lang-btn" data-lang="it">IT</button>
    </div>

    <!-- English Content -->
    <section id="content-en" class="content-lang">
      <header>
        <h1>Ephrem Bzuayehu Asmamawu</h1>
        <div class="contact-info">
          <span>📞 +39 3514683625</span>
          <span>✉️ <a href="mailto:ebzuayehu21@gmail.com">ebzuayehu21@gmail.com</a></span>
          <span>🔗 <a href="https://linkedin.com/in/ephremasmamawu">LinkedIn</a></span>
          <span>🏠 Via G.B. Tiepolo, 54, 35129 Padova, Italy</span>
        </div>
        <a href="cv-italian.pdf" class="download-btn" download>
          📥 Download CV (PDF)
        </a>
      </header>

      <article>
        <h2>About Me</h2>
        <p>I am a master’s student in Energy Engineering with a background in Mechanical Engineering and practical experience in computer-based vehicle systems and advanced energy modeling tools. I am passionate about renewable energy and sustainability, with a strong focus on energy efficiency and innovative technologies. My goal is to apply my skills to develop solutions that contribute to a more sustainable and energy-efficient future.</p>
      </article>

      <article>
        <h2>Experience</h2>
        <div class="section-item">
          <h3 class="job-title">Vehicle Engine Inspector</h3>
          <span class="date-location">General Motor Shear Company | Jan 2021 – Aug 2022</span>
          <ul>
            <li>Diagnosed and maintained vehicle electrical systems using advanced tools</li>
            <li>Specialized in troubleshooting automotive components</li>
          </ul>
        </div>
      </article>

      <!-- Other sections follow similar structure -->
      
    </section>

    <!-- Italian Content -->
    <section id="content-it" class="content-lang" hidden>
      <!-- Italian content structure mirroring English version -->
    </section>
  </div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const langButtons = document.querySelectorAll('.lang-btn');
    const contentSections = document.querySelectorAll('.content-lang');
    
    function setActiveLanguage(lang) {
      // Update UI
      langButtons.forEach(btn => {
        btn.classList.toggle('active', btn.dataset.lang === lang);
      });
      
      // Show/hide content
      contentSections.forEach(section => {
        section.hidden = section.id !== `content-${lang}`;
      });
      
      // Store preference
      localStorage.setItem('preferredLanguage', lang);
    }

    langButtons.forEach(btn => {
      btn.addEventListener('click', () => setActiveLanguage(btn.dataset.lang));
    });

    // Initialize
    const preferredLang = localStorage.getItem('preferredLanguage') || 'en';
    setActiveLanguage(preferredLang);
  });
</script>
</body>
</html>
